<% if logged_in? %>
  <h1 class="text-center mb-3">新着投稿</h1>
  <%= render "posts/posts", posts: @posts %>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Welcome to Flamme</h1>
      <%= link_to "新規会員登録", signup_path, class: "btn btn-lg btn-primary" %>
    </div>
  </div>
<% end %>